{
  "version": 2,
  "buildCommand": "cd api && npm install",
  "builds": [
    {
      "src": "api/query.js",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/api/query",
      "dest": "/api/query.js"
    }
  ]
}